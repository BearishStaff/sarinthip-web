# Project Context & Requirements (Sarinthip Web)

เอกสารนี้จัดทำขึ้นเพื่อให้ AI และผู้ร่วมพัฒนาคนอื่นๆ ที่เข้ามาพัฒนาหรือปรับปรุงระบบ สามารถเข้าใจบริบทของโปรเจกต์ ปัญหาที่แก้ไข และความต้องการหลักทั้งหมดของระบบ **sarinthip-web** ได้ทันที

---

## 📌 1. บริบทและที่มาของโปรเจกต์ (Background & Context)
- **กลุ่มเป้าหมาย:** เจ้าของธุรกิจ SME ที่มีหลายสาขา (ปัจจุบันมี 2 สาขาหลัก: **ร้านศรินทิพย์** ขายก๋วยเตี๋ยว/ข้าวหน้าเนื้อ และ **ร้าน PorPor** ขายบัวลอยน้ำขิง/ชา) และมีแผนขยายสาขาในอนาคต
- **ปัญหาปัจจุบัน (Pain Point):** เดิมทีลูกค้าใช้ Line Chatbot ชื่อ "Zendai" ในการบันทึกรายจ่ายและส่งสลิป แต่ติดข้อจำกัดด้านค่าใช้จ่าย หากส่งสลิปหรือทำรายการเกิน 30 รายการต่อเดือน จะมีค่าบริการเพิ่มขึ้น (300-600 บาท/เดือน) ซึ่งไม่คุ้มค่าในระยะยาว
- **เป้าหมายของระบบ (Goal):** สร้างเว็บแอปพลิเคชันระบบบัญชีภายในของตัวเอง เพื่อลดต้นทุนและรองรับการเก็บข้อมูลรายรับ-รายจ่ายที่ไม่มีข้อจำกัด ตลอดจนสามารถขยายฟังก์ชันต่างๆ ได้ตามต้องการ เช่น การกรอกข้อมูลแบบอัจฉริยะ (Smart Input) และการออกรายงานเพื่อนำไปยื่นภาษีสรรพากร

---

## 🎯 2. ความต้องการหลักของระบบ (Core Requirements - MVP)
- **การออกแบบ UI/UX:**
  - รองรับการใช้งานบนมือถือเป็นหลัก (Mobile-First / Responsive)
  - หน้าตาเรียบง่าย สวยงาม ทันสมัย และโหลดใช้งานได้รวดเร็ว
  - รองรับภาษาไทย 100%
- **การจัดการสาขา (Branch Management):**
  - รองรับการมีหลายสาขา
  - ผู้ใช้งานสามารถเพิ่ม/ลบ หรือแก้ไขสาขาได้จากหน้าแอปพลิเคชัน
  - สามารถเลือกเพื่อกรองข้อมูลรายรับ-รายจ่ายดูแยกรายสาขา หรือแสดงยอดขายรวมได้
- **ระบบบันทึกรายรับ (Income):**
  - บันทึกยอดขายประจำวันโดยแยกตามช่องทางการขาย (เช่น เงินสด/หน้าร้าน, Grab, Lineman, Foodpanda)
  - **Auto GP Calculation:** ระบบต้องหักค่า GP อัตโนมัติสำหรับยอดขายที่มาจาก Delivery Channels (ตามสัดส่วนที่ระบุ) เพื่อให้ได้ "ยอดสุทธิ (Net Amount)"
- **ระบบบันทึกรายจ่าย (Expense):**
  - มีระบบกรอกรายจ่ายแบบดั้งเดิม (Manual Form) และระบบแปลงข้อความอัตโนมัติ (Smart Bill Input)
  - **Smart Bill Input:** ลูกค้าคุ้นเคยกับการพิมพ์รายการยาวๆ หรือการส่งรายละเอียดบิลทีละหลายๆ บรรทัด เช่น `รายการ | จำนวน | หน่วย | ราคาต่อหน่วย` หรือ `หมูชิ้น 5 กิโล 120` ระบบจะแปลงข้อความชุดนี้เป็นรายการบันทึกแยกแถวอัตโนมัติ
  - **การแบ่งหมวดหมู่ (Category Auto-Matching):** จัดกลุ่มประเภทรายจ่าย (เช่น เนื้อสัตว์, ผัก, แก๊ส, ค่าแรง) อัตโนมัติ โดยอ้างอิงจากคีย์เวิร์ด (Keywords) ในตาราง `categories`
- **ระบบแดชบอร์ดสรุปผล (Dashboard):**
  - สรุปยอดรวมรายรับ ยอดรวมรายจ่าย และกำไร/ขาดทุนสุทธิ
  - ใช้ State Management ผ่าน URL Query Params (`?branchId=...&month=...`)
- **การออกรายงานภาษี (PDF Export):**
  - ต้องการเอกสาร **"ใบรับรองแทนใบเสร็จรับเงิน"** รูปแบบ A4 สรุปรายเดือน
  - ใช้เครื่องมือ `@react-pdf/renderer` ในการสร้างไฟล์ PDF ฝั่ง Client
  - รองรับฟอนต์ภาษาไทยมาตรฐานที่สรรพากรยอมรับ (Google Font: Sarabun)
  - จัดการการตัดหน้ากระดาษ (Pagination Logic) อย่างระมัดระวัง: ยึดหลักให้รายการของวันที่เดียวกันอยู่ในหน้าเดียวกันเสมอหากเนื้อที่ในหน้านั้นๆ ไม่พอ เพื่อไม่ให้ตารางแสดงผลแยกหน้าอย่างไม่สวยงาม

---

## 📂 3. โครงสร้างฐานข้อมูล (Database Schema - Supabase PostgreSQL)
ฐานข้อมูลทำงานบน Supabase (PostgreSQL) ประกอบด้วยตารางหลักดังนี้:
1. **`branches`**: เก็บข้อมูลสาขา (`id`, `name`, `created_at`, `deleted_at`)
2. **`categories`**: หมวดหมู่ค่าใช้จ่าย (`id`, `name`, `keywords` array ของคำค้นหาเพื่อใช้จับคู่ Smart Input)
3. **`bills`**: หัวกระดาษหรือเอกสารบิลรายจ่าย (`id`, `branch_id`, `billing_date`, `source`, `is_smart_input`)
4. **`expenses`**: รายการค่าใช้จ่ายย่อยในแต่ละบิล (`id`, `branch_id`, `bill_id`, `category_id`, `entry_date`, `item_name`, `qty`, `unit`, `price_per_unit`, `total_amount`)
5. **`income`**: บันทึกรายรับ (`id`, `branch_id`, `entry_date`, `amount`, `channel`, `gp_rate`, `note`)

---

## 🛠 4. แผนงานในอนาคต (Future Scope)
- **OCR Slip Processing:** ระบบอ่านและวิเคราะห์ภาพถ่ายสลิปรายจ่ายโดยอัตโนมัติ (สแกนสลิปแล้วดึงข้อมูลรายการและราคากรอกเข้าระบบ) โดยมีแผนจะเชื่อมต่อกับ Google Cloud Vision API
- **ระบบสิทธิ์ผู้ใช้งาน (Authentication & Roles):** จำกัดสิทธิ์ให้ลูกน้องหรือพนักงานหน้าร้านสามารถเพิ่มเฉพาะรายการของตนเองได้ โดยมีสิทธิ์ของผู้บริหาร (Admin) ในการตรวจดูแดชบอร์ดภาพรวมและออกรายงาน PDF
- **การย้าย Schema ไปยัง `branch_management`:** ย้ายข้อมูลจาก schema `public` ไปยัง schema เฉพาะเพื่อความปลอดภัยตามมาตรฐานความปลอดภัยที่ดีของ Supabase
